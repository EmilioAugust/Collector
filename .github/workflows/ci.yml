name: Run Tests for Backend
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: ${{ secrets.URL_DB }}
      CLEAN_DATABASE_URL: ${{ secrets.URL_CLEAN_DB }}
      DEBUG: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"
      
      - name: Install dependencies
        run: pip install -r requirements.txt  # Убрали cd backend
      
      - name: Run Tests
        run: pytest -v --tb=short  # Убрали cd backend
      
      - name: Debug (опционально)
        if: failure()
        run: |
          echo "Current directory: $(pwd)"
          echo "Tests directory:"
          ls -la tests/